function describe_city(city: string, country="Pakistan"): void {
    console.log(`${city} is in ${country}`);
}
// call the function for three cities, using the default country for two of them 
describe_city("Karachi");
